[project]
name = "sts2-mcp"
version = "0.1.0"
description = "MCP server bridge for Slay the Spire 2"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.7.1",
    "httpx>=0.25.0",
]

[project.scripts]
sts2-mcp = "server:main"
